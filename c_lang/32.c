#include<stdio.h>
int main()
 // array = a data structure that can store many values of the same data type.
{
    double prices[20] = {3.4, 8.2, 44, 23.7, 10};
    printf("Rs%.2lf",prices[4]);
    return 0;
}